name: CI

on:
  schedule:
    - cron: '0 12 * * 2'
    
  workflow_dispatch:

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: androidsdk/android-30:latest
    steps:
      - name: Log the parent container node version
        run: |
          pwd
          echo "complete"
